---
import { SITE_TITLE } from '../consts';

interface Props {
	showNav?: boolean;
}

const { showNav = true } = Astro.props;
---

<header
	class="z-header max-sm:static sm:sticky sm:top-0 bg-white shadow-header max-sm:shadow-none"
>
	<nav
		class="mx-auto flex max-w-wide items-center justify-between px-content py-content max-sm:flex-wrap max-sm:gap-element max-sm:py-element"
	>
		<a
			href="/"
			class="flex items-center gap-element font-heading text-h4 font-bold text-text no-underline transition-colors duration-fast hover:text-accent max-sm:text-body"
		>
			<img src="/images/logo.png" alt="" width="28" height="28" />
			{SITE_TITLE}
		</a>
		{showNav && (
			<div class="flex items-center gap-block max-sm:contents">
				<div
					class="flex gap-content font-heading text-small max-sm:order-1 max-sm:w-full max-sm:flex-wrap max-sm:gap-element"
				>
					<a href="/" class="nav-link">ホーム</a>
					<a href="/#plans" class="nav-link">プラン</a>
					<a href="/#access" class="nav-link">アクセス</a>
					<a href="/#faq" class="nav-link">Q&amp;A</a>
					<a href="/blog" class="nav-link">ブログ</a>
				</div>
			</div>
		)}
	</nav>
</header>

<style>
	.nav-link {
		font-weight: var(--font-weight-bold);
		color: var(--color-text);
		text-decoration: none;
		transition: color var(--duration-fast) ease;
	}
	.nav-link:hover {
		color: var(--color-accent);
	}
</style>
